import 'package:flutter/material.dart';

class Film_add extends StatelessWidget {
  const Film_add({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
