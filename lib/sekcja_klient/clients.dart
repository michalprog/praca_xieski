import 'package:flutter/material.dart';
class Clients_list extends StatelessWidget {
  const Clients_list({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
