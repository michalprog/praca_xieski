import 'package:flutter/material.dart';
class Client_remove extends StatelessWidget {
  const Client_remove({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
